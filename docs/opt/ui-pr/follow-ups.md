# Phase 4 UI/UX Follow-ups

本文件只记录在 PR 实施过程中被明确降级的非阻塞事项。任何条目必须写清触发条件、owner、关闭 PR 和验证方式；不得把当前 PR 的 P0/P1 出口门禁挪到这里。

当前无 deferred follow-up。禁止把任一 PR 的 P0/P1 出口门禁降级到本文件。

| 条目 | 触发条件 | owner | 关闭 PR | 验证方式 |
| --- | --- | --- | --- | --- |
