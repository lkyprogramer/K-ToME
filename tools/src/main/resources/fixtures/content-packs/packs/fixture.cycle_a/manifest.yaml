id: fixture.cycle_a
version: 1.0.0
schemaVersion: 1
gameVersionRange: ">=0.1.0 <0.2.0"
namespace: fixture_cycle_a
dependencies:
  - id: fixture.cycle_b
    versionRange: ">=1.0.0 <2.0.0"
overlays:
  - targetRef:
      registry: monster
      id: fixture.cycle_a.watcher
    op: ADD
    sourceFile: data/monsters/fixture.cycle_a.watcher.yaml
