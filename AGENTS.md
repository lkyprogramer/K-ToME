# K-ToME AGENTS Guide

本文件用于约束本仓库后续的人类与 AI 协作开发方式。目标不是增加流程负担，而是把项目长期可维护性、模块边界与可验证性固定下来，避免在迭代中逐步退化。

## 1. 项目简介

K-ToME 是一个使用 `Kotlin + libGDX` 开发的类 ToME 回合制 Roguelike 项目。

当前阶段目标不是“快速堆系统”，而是先建立一条可以长期演进的主线：

`零引擎依赖 core -> 可运行 client 壳层 -> 可验证战斗与成长循环 -> 完整 MVP -> 可发布版本`

长期规划与完整目标说明见：

- [docs/mvp-development-guide.md](docs/mvp-development-guide.md)
- [docs/phase1/2026-03-12-phase1-roadmap.md](docs/phase1/2026-03-12-phase1-roadmap.md)

阶段性实现、覆盖率与出口要求，以对应 phase 文档为准；本文件负责固定全局开发原则、模块边界和质量要求。

## 2. 开发前强制预检

对任何 `moderate / complex` 任务，开始实现前必须完成预检。

### 2.1 必读文档

至少阅读以下内容：

1. [docs/mvp-development-guide.md](docs/mvp-development-guide.md)
2. 当前阶段路线图，例如 [docs/phase1/2026-03-12-phase1-roadmap.md](docs/phase1/2026-03-12-phase1-roadmap.md)
3. 当前任务命中的阶段文档，例如 `docs/phase1/2026-03-12-phase1-*.md`
4. 本次会修改到的代码与对应测试

如果任务只涉及简单局部修复，可以不重复通读全部文档，但仍必须确认本次改动不会破坏模块边界和当前阶段冻结约束。

### 2.2 第一条输出要求

对非 trivial 任务，第一条输出必须包含简短《预检摘要》，至少说明：

1. 本次任务命中的阶段与文档
2. 受影响模块与各模块权威职责
3. 关键约束与风险
4. 计划执行的测试 / 验证命令

如果文档缺失、约束冲突、或用户要求与阶段冻结目标明显矛盾，必须先指出冲突，不能直接实现。

## 3. 架构基线

### 3.1 基本原则

1. `core` 是规则真源，且必须保持零引擎依赖。
2. `game` 负责内容装配、数据注册、配置与胶水逻辑。
3. `client` 只负责渲染、输入、窗口、音频和画面编排。
4. 任何游戏规则都不能为了“方便渲染”回流到 `client`。
5. 当前阶段优先保证“可测试、可推理、可白盒验证”，而不是追求性能型优化。

### 3.2 模块责任矩阵

#### `core`

职责：

1. ECS、地图、FOV、移动、战斗、AI、寻路、属性、物品、天赋、存档等规则逻辑
2. 纯数据结构、纯函数、确定性算法
3. 可被 JUnit 直接验证的所有核心行为

禁止：

1. 依赖 libGDX、Lua、GUI、音频、窗口或任何渲染框架
2. 依赖平台 IO、输入事件、屏幕尺寸等客户端概念
3. 为了方便界面而混入渲染状态

#### `game`

职责：

1. 从配置、内容定义或未来的 YAML / Lua / 注册表组装游戏内容
2. 把 `core` 的规则能力装配成具体玩法会话
3. 管理“官方游戏内容”与规则参数，而不是重写规则引擎

禁止：

1. 实现本应属于 `core` 的公式、判定、AI 规则
2. 承担渲染、输入、窗口或相机逻辑
3. 绕过 `core` 自行维护权威世界状态

#### `client`

职责：

1. 桌面入口、窗口生命周期、输入采集、渲染、viewport、相机、音频
2. 把玩家输入转换为对 `game/core` 的调用
3. 把 `game/core` 产出的状态映射为可见表现

禁止：

1. 地图生成、战斗结算、视野算法、寻路、AI 决策直接写在 `client`
2. 在 `client` 中保存规则权威状态的独立副本
3. 因渲染需要修改 `core` 的语义边界

### 3.3 模块依赖规则

只允许以下依赖方向：

1. `root -> core / game / client`
2. `game -> core`
3. `client -> game`
4. `client -> core` 仅限当前薄壳阶段的直接消费；长期应优先经 `game` 暴露的会话边界访问

不允许：

1. `core -> game`
2. `core -> client`
3. `game -> client`
4. 通过共享可变状态绕过模块边界

## 4. 核心开发规范

### 4.1 规则归属与抽象边界

1. 规则先放进 `core`，表现层再消费结果；不要先把逻辑写进 `client`，再“抽回” `core`。
2. 新增系统前先判断它属于“规则”“内容”还是“表现”，不要创建模糊中间层。
3. 如果某段逻辑需要固定 seed 才能验证，它大概率应该在 `core` 而不是 `client`。
4. 公共类型一旦被多个模块消费，必须优先保证语义稳定和命名清晰。

### 4.2 纯度、确定性与可推理性

1. 地图生成、FOV、战斗公式、AI 决策、掉落计算等，默认都应支持固定 seed 或可注入随机源。
2. 只要逻辑可以写成纯函数，就不要引入隐藏状态。
3. 时间、随机数、输入、文件系统都应显式注入或隔离，不允许在规则核心里偷偷读取全局环境。
4. 不要把“当前阶段还没用到”的性能优化提前做进基础设施。

### 4.3 代码风格

1. 优先小而可审阅的改动，不做无关重构。
2. 命名必须体现业务/规则语义，避免 `Manager`、`Helper`、`Utils` 泛化命名泛滥。
3. 注释只解释“为什么这样做”，不复述代码本身。
4. 非必要不要引入反射、全局单例、复杂缓存、隐式生命周期。
5. 变更公共 API、存档格式、数据模型、跨模块契约时，必须明确列出影响面。

### 4.4 当前阶段冻结要求

Phase 1 内默认遵守以下限制，除非对应阶段文档明确开放：

1. 不引入 Lua mod runtime
2. 不引入网络联机
3. 不引入复杂 UI 框架
4. 不引入正式 tile 管线
5. 不为了 ECS 性能做过早架构优化

## 5. 测试规范

### 5.1 总原则

1. 所有核心规则优先以单元测试自证，而不是靠手动跑客户端观察。
2. 新功能不是“先写代码再补测试”，而是规则与测试同步交付。
3. 对 `core` 的非平凡改动，没有对应测试视为未完成。

### 5.2 `core` 测试要求

`core` 中以下类型的改动必须补或改测试：

1. ECS 行为变更
2. 地图生成、FOV、移动、战斗、AI、掉落、升级等规则变更
3. 任何带分支、状态转移、边界条件或失败路径的逻辑

要求：

1. 使用固定 seed、固定输入、固定输出断言
2. 覆盖 happy path、边界条件、失败路径和回归场景
3. 测试不得依赖 GUI、帧率、睡眠时间、人工交互
4. 对随机算法，必须断言“确定性”和关键不变量，而不是只断言“看起来像”

### 5.3 `game` 测试要求

1. 验证装配结果而不是重复测试 `core` 算法细节
2. 关注配置到运行态对象的映射是否正确
3. 新增内容系统时，应优先验证注册、装配和默认参数

### 5.4 `client` 测试要求

1. 当前阶段不追求复杂 GUI 自动化测试
2. `client` 变更至少要保证编译通过，并有可重复的手动白盒步骤
3. 若能把输入映射、视口计算等提取为纯逻辑，应补局部单测而不是只靠手动验证

### 5.5 覆盖率要求

覆盖率口径以阶段文档为准，当前至少遵守：

1. `core` 关键包覆盖率必须满足 phase 文档门禁
2. `jacocoTestCoverageVerification` 失败不能忽略
3. 不能为了冲覆盖率写无效测试；必须优先覆盖真实规则分支

## 6. 验证规范

### 6.1 自动化验证

提交前至少执行与本次改动匹配的命令。当前基础命令为：

```bash
./gradlew test
./gradlew :core:test
./gradlew jacocoTestReport
./gradlew :core:jacocoTestCoverageVerification
./gradlew check
```

如果当前机器存在 Maven / TLS 拉取问题，可先执行：

```bash
./scripts/bootstrap-deps.sh
```

该脚本会在 `.bootstrap/m2` 下生成本地仓库，并同步准备与 wrapper 匹配的本地 Gradle 发行版。构建会优先使用该仓库。`.bootstrap/` 是本地生成物，不应提交。

GitHub CI 的默认自动化入口与本地验证保持一致：

1. `.github/workflows/ci.yml` 在 PR 和 `main` 分支推送上执行 `./gradlew --no-daemon check`
2. `.github/workflows/bootstrap-verify.yml` 仅在构建脚本、依赖声明、wrapper 或 bootstrap 脚本变化时执行 `./scripts/verify-bootstrap.sh --clean`
3. `.github/workflows/dependency-submission.yml` 在 `main` 分支推送时提交依赖图，供 GitHub 依赖分析使用

如果本次改动涉及以下任一内容，必须额外执行一次 bootstrap 离线烟雾验证：

1. `gradle.properties`
2. `build.gradle.kts` / `settings.gradle.kts`
3. `scripts/bootstrap-deps.sh`
4. 任何依赖版本调整

验证命令：

```bash
./scripts/verify-bootstrap.sh
```

该验证必须使用 bootstrapped Gradle binary 和隔离的 `.bootstrap/gradle-user-home`，避免因为 runner 或本机已有 `~/.gradle` 缓存而产生假阳性。

### 6.2 白盒验证

凡是涉及 `client`、渲染、输入、交互流程的改动，都必须保留明确白盒步骤，至少说明：

1. 启动命令
2. 操作步骤
3. 预期结果
4. 证据位置或观察点

例如当前基础切片的白盒命令为：

```bash
./gradlew :client:run
```

需要验证：

1. 地牢是否渲染
2. 玩家 `@` 是否能移动
3. 墙体阻挡是否正确
4. FOV 是否刷新
5. 不可见区域是否泄露信息
6. 视口 / 相机是否稳定

### 6.3 验证结果汇报

完成任务后必须真实说明：

1. 实际运行了哪些命令
2. 哪些命令未运行以及原因
3. 白盒验证是否实际执行
4. 仍然存在的风险或未覆盖点

禁止把“理论上应该通过”当作已经验证。

## 7. 文档与变更同步

1. 如果变更影响架构边界、模块职责、构建方式、测试入口或阶段出口，必须同步更新相关文档。
2. 如果 `AGENTS.md` 发生变化，应顺手评估 `docs/mvp-development-guide.md`、阶段路线图或验证文档是否需要同步。
3. 如果新增目录承载明确职责，建议同步补简短 README，防止结构语义只存在于代码里。

## 8. 高风险变更红线

以下变更不能静默做掉，必须先明确影响：

1. 存档格式变化
2. 公共模型或跨模块契约变化
3. 把规则从 `core` 挪到 `game/client`
4. 引入引擎类型进入 `core`
5. 删除 coverage gate、跳过测试门禁、绕过白盒验证
6. 用“临时逻辑”污染长期架构边界

## 9. DoD（完成定义）

一次合格提交，至少应满足：

1. 命中的阶段文档已阅读并遵守
2. 模块边界未被破坏
3. 新规则逻辑已有测试
4. 对应自动化命令已运行并报告结果
5. 涉及 `client` 的改动已有白盒验证步骤
6. 文档与验证说明已同步
7. 没有把当前阶段非目标偷偷带入主干实现

## 10. 一句话原则

当你不确定代码该放在哪一层时，先问自己一句：

“这段代码是在定义游戏规则，还是在展示/驱动游戏规则？”

如果答案是前者，默认放进 `core`；如果答案是后者，默认留在 `game` 或 `client`，且不要反向污染规则层。
